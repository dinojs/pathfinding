import React, { Component } from "react";

adjNodes = (nodes) => {
    nodes[3]
}





bfs = (start = 30979237) => {
    let frontier = []
    frontier.push[start]
    let came_from = {}
    came_from[start] = 'None'

    while (frontier.length) {
        //removes the first item of an array
        let current = frontier.shift()
        for (let i in came_from)
    }

}


export function renderLayers(props) {
    const { nodes } = props;

    return [

    ]
}
